<Page x:Class="UR.WP81.Views.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:UR.WP81.Views"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:viewModels="using:UR.WP81.ViewModels"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance Type=viewModels:MainPageViewModel, IsDesignTimeCreatable=False}"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel VerticalAlignment="Center">
            <Button Content="Start"
                    IsEnabled="{Binding IsStarted, Converter={StaticResource CBoolNotBool}}"
                    HorizontalAlignment="Center"
                    x:Name="StartCommand" />

            <Button Content="Stop"
                    IsEnabled="{Binding IsStarted}"
                    HorizontalAlignment="Center"
                    x:Name="StopCommand" />
        </StackPanel>


        <Button Content="track list"
                VerticalAlignment="Bottom"
                IsEnabled="{Binding IsBusy, Converter={StaticResource CBoolNotBool}}"
                HorizontalAlignment="Center"
                x:Name="ViewTrackListCommand" />
    </Grid>
</Page>
